"""External managed LLM API client scaffold (no self-hosted inference)."""


def generate_response(prompt: str) -> str:
    """Return a placeholder model response."""
    return f"[placeholder-response] {prompt}"
